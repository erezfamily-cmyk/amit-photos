# Quiz Game "מאיפה הצילום?" Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a 5-question "מאיפה הצילום?" quiz game that awards a 20% discount on the full gallery to players who answer 3+/5 correctly.

**Architecture:** Standalone `quiz/index.html` fetches `quiz_eligible` photos from the existing `/api/photos` API, runs a 5-question quiz, then redirects to `/?discount=quiz`. The gallery's `getEffectivePrice()` already handles one-photo puzzle discounts — we extend it to handle an all-photos quiz discount. All new DB fields go through the existing PATCH endpoint. The admin panel gets a 🌍 button+badge matching the existing 🧩 puzzle pattern.

**Tech Stack:** Vanilla JS/HTML/CSS (no frameworks), Cloudflare D1 (SQLite), Cloudflare Worker (existing `worker.js`), Python + Anthropic SDK (scan script), GitHub Actions (weekly promo)

---

## File Map

| File | Action | Responsibility |
|------|--------|----------------|
| `worker.js` | Modify | PATCH: accept `quiz_eligible`/`quiz_description`; GET: include these fields |
| `assets/js/gallery.js` | Modify | Detect `?discount=quiz`, apply 20% to all photos, show 🌍 badge in modal |
| `admin.html` | Modify | 🌍 badge + copy-link button + long-press toggle for `quiz_eligible` |
| `quiz/index.html` | Create | Standalone quiz game (HTML+CSS+JS, no deps) |
| `src/scan_quiz_photos.py` | Create | Claude Vision one-time scan — marks `quiz_eligible` + generates `quiz_description` |
| `src/quiz_social_post.py` | Create | Weekly promo: picks eligible photo, Claude generates caption, posts to IG+FB |
| `.github/workflows/quiz-social-post.yml` | Create | Cron every Wednesday 15:00 UTC (18:00 Israel) |

---

## Task 1: D1 Migration — add quiz columns

**Files:**
- No new file — run SQL via Cloudflare dashboard or wrangler

- [ ] **Step 1: Run migration via wrangler**

```bash
# from repo root — this adds the two columns to the live D1 database
npx wrangler d1 execute amit-photos-db --remote --command "ALTER TABLE photos ADD COLUMN quiz_eligible INTEGER DEFAULT 0"
npx wrangler d1 execute amit-photos-db --remote --command "ALTER TABLE photos ADD COLUMN quiz_description TEXT DEFAULT ''"
```

Expected output:
```
✅  Executed successfully
```

If wrangler is not configured locally, run these two SQL statements in the Cloudflare dashboard → D1 → amit-photos-db → Console.

- [ ] **Step 2: Verify columns exist**

```bash
npx wrangler d1 execute amit-photos-db --remote --command "PRAGMA table_info(photos)" | grep -E "quiz_eligible|quiz_description"
```

Expected: two rows with `quiz_eligible` (type INTEGER) and `quiz_description` (type TEXT).

- [ ] **Step 3: Commit a migration note**

```bash
git add -A
git commit -m "chore: add quiz_eligible + quiz_description columns to D1"
```

---

## Task 2: worker.js — expose quiz fields in API

**Files:**
- Modify: `worker.js` (lines ~283–331, the photos GET + PATCH handlers)

The GET `/api/photos` maps DB rows → JSON. The PATCH `/api/photos` currently handles `published`, `title`, `category`, `description`. We need to also handle `quiz_eligible` and `quiz_description`.

- [ ] **Step 1: Expose fields in GET response**

Find the section where photos rows are mapped (around line 283). It currently spreads `p` directly, so the new DB columns will appear automatically in the response — no code change needed here. Verify by checking the GET handler:

```javascript
// Current code around line 289 (worker.js):
const photos = results.map(p => ({
  ...p,
  is_week_photo: !!(weekPhotoId && p.id === weekPhotoId),
  // ...
}));
```

Since `quiz_eligible` and `quiz_description` are DB columns, they appear in `...p` automatically. **No change needed for GET.**

- [ ] **Step 2: Extend PATCH to handle quiz fields**

Find the PATCH block (line 307 in worker.js). Currently:
```javascript
if (method === 'PATCH') {
  const { id, title, category, description, published } = await request.json().catch(() => ({}));
  if (!id) return jsonRes({ error: 'id חסר' }, 400);
```

Replace the entire PATCH block (lines 307–331) with:

```javascript
if (method === 'PATCH') {
  if (!await checkAuth(request, env)) return unauth(request);
  const body = await request.json().catch(() => ({}));
  const { id } = body;
  if (!id) return jsonRes({ error: 'id חסר' }, 400, request);

  if (body.published !== undefined) {
    await env.DB.prepare('UPDATE photos SET published=? WHERE id=?').bind(body.published ? 1 : 0, id).run();
    return jsonRes({ ok: true, published: body.published ? 1 : 0 }, 200, request);
  }

  if (body.quiz_eligible !== undefined || body.quiz_description !== undefined) {
    const current = await env.DB.prepare('SELECT quiz_eligible, quiz_description FROM photos WHERE id=?').bind(id).first();
    if (!current) return jsonRes({ error: 'תמונה לא נמצאה' }, 404, request);
    const newEligible = body.quiz_eligible !== undefined ? (body.quiz_eligible ? 1 : 0) : current.quiz_eligible;
    const newDesc     = body.quiz_description !== undefined ? body.quiz_description : current.quiz_description;
    await env.DB.prepare('UPDATE photos SET quiz_eligible=?, quiz_description=? WHERE id=?').bind(newEligible, newDesc, id).run();
    return jsonRes({ ok: true, quiz_eligible: newEligible, quiz_description: newDesc }, 200, request);
  }

  let finalTitle = body.title || '';
  if (isGenericTitle(finalTitle)) {
    const row = await env.DB.prepare('SELECT r2_key FROM photos WHERE id=?').bind(id).first();
    if (row?.r2_key) {
      const origin = new URL(request.url).origin;
      const aiTitle = await generateHebrewTitle(`${origin}/photos/${row.r2_key}`, body.category || '', env);
      if (aiTitle) finalTitle = aiTitle;
    }
  }
  await env.DB.prepare(
    'UPDATE photos SET title=?,category=?,description=? WHERE id=?'
  ).bind(finalTitle, body.category || '', body.description || '', id).run();
  return jsonRes({ ok: true, title: finalTitle }, 200, request);
}
```

- [ ] **Step 3: Deploy worker**

```bash
npx wrangler deploy
```

Expected:
```
✅  Deployed worker to https://amit-photos.workers.dev
```

- [ ] **Step 4: Smoke-test PATCH quiz_eligible**

```bash
# Replace TOKEN with a valid session token from admin login
curl -X PATCH https://amitphotos.com/api/photos \
  -H "Content-Type: application/json" \
  -H "X-Session-Token: TOKEN" \
  -d '{"id":"SOME_PHOTO_ID","quiz_eligible":true,"quiz_description":"הקולוסיאום — אמפיתיאטרון רומי. רומא, איטליה."}'
```

Expected: `{"ok":true,"quiz_eligible":1,"quiz_description":"..."}`

- [ ] **Step 5: Commit**

```bash
git add worker.js
git commit -m "feat: worker PATCH accepts quiz_eligible + quiz_description"
```

---

## Task 3: gallery.js — quiz discount support

**Files:**
- Modify: `assets/js/gallery.js` (state variables ~line 37, `handleInitialHash` ~line 878, `getEffectivePrice` ~line 1195, `openBuyModal` ~line 1283)

- [ ] **Step 1: Add state variable**

After line 38 (`const PUZZLE_DISCOUNT = 0.20;`), add:

```javascript
let quizDiscountActive = false; // set when arriving from quiz with ?discount=quiz
const QUIZ_DISCOUNT = 0.20;
```

- [ ] **Step 2: Detect `?discount=quiz` in handleInitialHash**

In `handleInitialHash()` (line 878), the first `if` block checks `urlParams.get('discount') === 'puzzle'` and then `return`s. Add a parallel block immediately before the `return` of the puzzle block, or as its own `if` at the top of the function:

After the puzzle block ends (line 910, after the `return;`), add:

```javascript
  if (urlParams.get('discount') === 'quiz') {
    quizDiscountActive = true;
    const banner = document.getElementById('puzzle-discount-banner');
    if (banner) {
      banner.classList.remove('hidden');
      // Replace puzzle text with quiz text
      const bannerTitle = banner.querySelector('.puzzle-discount-title');
      const bannerSub   = banner.querySelector('.puzzle-discount-sub');
      if (bannerTitle) bannerTitle.textContent = '🌍 הנחת קוויז 20%';
      if (bannerSub)   bannerSub.textContent   = 'ניצחת במשחק — ההנחה בתוקף על כל התמונות';
    }
    return;
  }
```

- [ ] **Step 3: Apply quiz discount in getEffectivePrice**

In `getEffectivePrice()` (line 1206), after the puzzle discount block, add:

```javascript
  if (quizDiscountActive) {
    return Math.round(globalPrices[size] * (1 - QUIZ_DISCOUNT));
  }
```

The function after this change:

```javascript
function getEffectivePrice(photoId, size) {
  const photo = allPhotos.find(p => p.id === photoId);
  if (photo?.price_overrides) {
    try {
      const ov = typeof photo.price_overrides === 'string' ? JSON.parse(photo.price_overrides) : photo.price_overrides;
      if (ov[size] != null) return ov[size];
    } catch {}
  }
  if (photo?.is_week_photo && photo?.week_photo_discount) {
    return Math.round(globalPrices[size] * (1 - photo.week_photo_discount));
  }
  if (puzzleDiscountPhotoId && String(photoId) === String(puzzleDiscountPhotoId)) {
    return Math.round(globalPrices[size] * (1 - PUZZLE_DISCOUNT));
  }
  if (quizDiscountActive) {
    return Math.round(globalPrices[size] * (1 - QUIZ_DISCOUNT));
  }
  return globalPrices[size];
}
```

- [ ] **Step 4: Show 🌍 badge in openBuyModal**

In `openBuyModal` (line ~1283), the badge text logic currently is:

```javascript
badge.textContent = photo.is_week_photo ? '⭐ תמונת השבוע' : (isPuzzle ? '🧩 הנחת פאזל' : '🏷 מבצע');
```

Replace with:

```javascript
badge.textContent = photo.is_week_photo
  ? '⭐ תמונת השבוע'
  : isPuzzle
    ? '🧩 הנחת פאזל'
    : quizDiscountActive
      ? '🌍 הנחת קוויז'
      : '🏷 מבצע';
```

Also update the discount label in the confirmation step (~line 1349):

```javascript
// Current:
document.getElementById('buy-discount-label').textContent = photo.is_week_photo ? '⭐ הנחת תמונת השבוע' : '🏷 הנחה';
// Replace with:
document.getElementById('buy-discount-label').textContent = photo.is_week_photo
  ? '⭐ הנחת תמונת השבוע'
  : puzzleDiscountPhotoId && String(photo.id) === String(puzzleDiscountPhotoId)
    ? '🧩 הנחת פאזל'
    : quizDiscountActive
      ? '🌍 הנחת קוויז'
      : '🏷 הנחה';
```

- [ ] **Step 5: Test manually**

Open browser: `http://localhost:8000/?discount=quiz`

Expected:
- Puzzle discount banner appears with quiz text
- Click any photo → buy modal → all sizes show discounted prices with `🌍 הנחת קוויז` badge

- [ ] **Step 6: Commit**

```bash
git add assets/js/gallery.js
git commit -m "feat: gallery supports ?discount=quiz — applies 20% to all photos"
```

---

## Task 4: admin.html — 🌍 badge + button + toggle

**Files:**
- Modify: `admin.html` (photo card render function, ~line 1562)

- [ ] **Step 1: Add 🌍 קוויז badge to card title**

In the photo card render function (line 1562), the title div currently is:

```javascript
<div class="photo-title">${p.title||'ללא כותרת'}${p.published===0?'<span class="draft-badge">טיוטה</span>':''}${isOnSale(p)?'<span class="sale-badge">🏷 מבצע</span>':''}${p.is_week_photo?'<span class="sale-badge" style="color:#f59e0b;background:rgba(245,158,11,.12);border-color:rgba(245,158,11,.35)">⭐ שבוע</span>':''}</div>
```

Replace with (add quiz badge at end):

```javascript
<div class="photo-title">${p.title||'ללא כותרת'}${p.published===0?'<span class="draft-badge">טיוטה</span>':''}${isOnSale(p)?'<span class="sale-badge">🏷 מבצע</span>':''}${p.is_week_photo?'<span class="sale-badge" style="color:#f59e0b;background:rgba(245,158,11,.12);border-color:rgba(245,158,11,.35)">⭐ שבוע</span>':''}${p.quiz_eligible?'<span class="sale-badge" style="color:#34d399;background:rgba(52,211,153,.1);border-color:rgba(52,211,153,.3)">🌍 קוויז</span>':''}</div>
```

- [ ] **Step 2: Add 🌍 copy-link button to photo actions**

In the photo actions row (line 1566), directly after the 🧩 puzzle button, add:

```javascript
<button class="btn-icon" title="העתק לינק הנחת קוויז 20%" onclick="event.stopPropagation();navigator.clipboard.writeText('https://amitphotos.com/?discount=quiz').then(()=>{this.textContent='✓';setTimeout(()=>{this.textContent='🌍';},1500)})">🌍</button>
```

- [ ] **Step 3: Add toggleQuiz function**

Find the Photos object / functions section in admin.html. Add a new function `toggleQuizEligible(photoId, currentState)` that calls PATCH:

```javascript
async function toggleQuizEligible(photoId, currentState) {
  const newState = !currentState;
  try {
    const resp = await fetch('/api/photos', {
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json', 'X-Session-Token': getSessionToken() },
      body: JSON.stringify({ id: photoId, quiz_eligible: newState }),
    });
    if (!resp.ok) throw new Error(await resp.text());
    const data = await resp.json();
    // update local photo data
    const photo = Photos.list.find(p => p.id === photoId);
    if (photo) photo.quiz_eligible = data.quiz_eligible;
    Photos.render();
    showToast(newState ? '🌍 סומן כמתאים לקוויז' : '🌍 הוסר מהקוויז');
  } catch(e) {
    showToast('שגיאה: ' + e.message);
  }
}
```

(If `showToast` doesn't exist, use `alert()`. If `getSessionToken()` doesn't exist, use `document.querySelector('[data-session-token]')?.dataset.sessionToken || localStorage.getItem('sessionToken') || ''`.)

- [ ] **Step 4: Wire double-click on 🌍 button to toggle**

Update the 🌍 button from Step 2 to support both copy (single-click) and toggle (double-click):

```javascript
<button class="btn-icon${p.quiz_eligible?' quiz-active':''}" 
  title="לחיצה: העתק לינק קוויז | לחיצה כפולה: ${p.quiz_eligible?'הסר מקוויז':'סמן לקוויז'}"
  onclick="event.stopPropagation();navigator.clipboard.writeText('https://amitphotos.com/?discount=quiz').then(()=>{this.textContent='✓';setTimeout(()=>{this.textContent='🌍';},1500)})"
  ondblclick="event.stopPropagation();toggleQuizEligible('${p.id}',${!!p.quiz_eligible})">🌍</button>
```

- [ ] **Step 5: Add CSS for quiz-active button state**

Find the `.btn-icon` CSS in admin.html and add:

```css
.btn-icon.quiz-active { color: #34d399; }
```

- [ ] **Step 6: Test manually**

Open admin → find a photo → click 🌍 (should copy link) → double-click 🌍 (should toggle quiz badge) → verify `🌍 קוויז` badge appears/disappears on card.

- [ ] **Step 7: Commit**

```bash
git add admin.html
git commit -m "feat: admin shows quiz badge + copy-link + toggle for quiz_eligible"
```

---

## Task 5: quiz/index.html — standalone quiz game

**Files:**
- Create: `quiz/index.html`

This is a single-file page (HTML + CSS + JS). It fetches `/api/photos`, filters `quiz_eligible: true` photos, picks 5 random ones, and runs the quiz.

- [ ] **Step 1: Create quiz/index.html**

Create `quiz/index.html` with the full content below:

```html
<!DOCTYPE html>
<html dir="rtl" lang="he">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>מאיפה הצילום? — Amit Photos</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Heebo:wght@400;600;700&family=Syne:wght@700&display=swap" rel="stylesheet">
<style>
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
:root {
  --bg: #0a0a0a;
  --surface: #111;
  --border: #222;
  --accent: #c8a96e;
  --green: #34d399;
  --red: #ef4444;
  --text: #fff;
  --muted: #888;
}
body {
  font-family: 'Heebo', sans-serif;
  background: var(--bg);
  color: var(--text);
  direction: rtl;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 1.5rem 1rem 3rem;
}
.site-header {
  width: 100%;
  max-width: 420px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 1.5rem;
}
.site-title {
  font-family: 'Syne', sans-serif;
  font-size: 1.1rem;
  color: var(--accent);
  text-decoration: none;
}
.game-card {
  width: 100%;
  max-width: 420px;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 16px;
  overflow: hidden;
  box-shadow: 0 20px 60px rgba(0,0,0,0.5);
}

/* Progress bar */
.progress-bar {
  background: #151515;
  padding: 0.6rem 1rem;
  display: flex;
  align-items: center;
  gap: 0.75rem;
  border-bottom: 1px solid #1f1f1f;
}
.progress-track { flex: 1; height: 4px; background: #2a2a2a; border-radius: 2px; }
.progress-fill  { height: 100%; background: var(--accent); border-radius: 2px; transition: width 0.3s; }
.progress-text  { color: var(--muted); font-size: 0.72rem; white-space: nowrap; }
.score-badge    { color: var(--accent); font-size: 0.72rem; font-weight: 700; white-space: nowrap; }

/* Photo */
.photo-area {
  position: relative;
  height: 210px;
  background: #1a1208;
  overflow: hidden;
}
.photo-area img { width: 100%; height: 100%; object-fit: cover; opacity: 0.88; display: block; }
.photo-overlay  { position: absolute; inset: 0; background: linear-gradient(to bottom, transparent 45%, var(--surface) 100%); }
.question-label {
  position: absolute;
  top: 10px; right: 10px;
  background: rgba(0,0,0,0.65);
  border: 1px solid #333;
  border-radius: 20px;
  padding: 3px 10px;
  font-size: 0.7rem;
  color: #aaa;
}

/* Photo description (shown after answer) */
.photo-desc {
  padding: 0.6rem 1rem;
  background: #141414;
  border-bottom: 1px solid #1e1e1e;
  font-size: 0.78rem;
  color: var(--muted);
  line-height: 1.5;
  display: none;
}
.photo-desc strong { color: var(--accent); }

/* Answer buttons */
.answers {
  padding: 0.75rem 0.9rem;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 0.45rem;
}
.answer-btn {
  background: #1a1a1a;
  border: 1px solid #2a2a2a;
  border-radius: 8px;
  padding: 0.6rem 0.5rem;
  text-align: right;
  cursor: pointer;
  font-size: 0.82rem;
  color: #ccc;
  font-family: 'Heebo', sans-serif;
  transition: border-color 0.1s, background 0.1s;
}
.answer-btn:hover:not(:disabled) { border-color: var(--accent); color: #fff; }
.answer-btn.correct { background: rgba(52,211,153,0.12); border-color: var(--green); color: var(--green); }
.answer-btn.wrong   { background: rgba(239,68,68,0.1);  border-color: var(--red);   color: var(--red);   }
.answer-btn:disabled { cursor: default; }

/* Feedback */
.feedback {
  margin: 0 0.9rem 0.85rem;
  padding: 0.6rem 0.85rem;
  border-radius: 8px;
  font-size: 0.79rem;
  line-height: 1.5;
  display: none;
}
.feedback.correct { background: rgba(52,211,153,0.09); border: 1px solid rgba(52,211,153,0.28); color: #4ade80; display: block; }
.feedback.wrong   { background: rgba(239,68,68,0.07);  border: 1px solid rgba(239,68,68,0.22);  color: #f87171; display: block; }

/* Next button */
.next-btn {
  display: none;
  width: calc(100% - 1.8rem);
  margin: 0 0.9rem 0.9rem;
  padding: 0.65rem;
  background: var(--accent);
  color: #111;
  border: none;
  border-radius: 8px;
  font-size: 0.88rem;
  font-weight: 700;
  font-family: 'Heebo', sans-serif;
  cursor: pointer;
}

/* ===== WIN / LOSE SCREEN ===== */
.result-screen { display: none; }
.win-photo {
  height: 170px; position: relative; overflow: hidden; background: #1a1208;
}
.win-photo img { width:100%;height:100%;object-fit:cover;opacity:0.75;display:block; }
.win-badge {
  position:absolute; top:10px; right:10px;
  background: var(--accent); color:#111;
  font-size:0.7rem; font-weight:700;
  padding:3px 10px; border-radius:20px;
}
.win-photo-overlay { position:absolute;inset:0;background:linear-gradient(to bottom,transparent 40%,var(--surface) 100%); }
.win-content { padding: 1rem 1rem 1.25rem; text-align: center; }
.win-trophy  { font-size: 2rem; margin-bottom: 0.3rem; }
.win-title   { font-size: 1.1rem; font-weight: 700; margin-bottom: 0.2rem; }
.win-sub     { color: var(--muted); font-size: 0.8rem; margin-bottom: 0.9rem; }
.win-score   { display:flex; justify-content:center; gap:0.6rem; margin-bottom:0.9rem; }
.win-dot {
  width:26px;height:26px; border-radius:50%;
  display:flex;align-items:center;justify-content:center;
  font-size:0.72rem; font-weight:700;
}
.win-dot.ok  { background:rgba(52,211,153,0.18); border:1.5px solid var(--green); color:var(--green); }
.win-dot.bad { background:rgba(239,68,68,0.12);  border:1.5px solid var(--red);   color:var(--red);   }
.discount-box {
  background: linear-gradient(135deg,rgba(200,169,110,0.13),rgba(200,169,110,0.04));
  border: 1px solid rgba(200,169,110,0.3);
  border-radius: 10px;
  padding: 0.7rem 0.9rem;
  margin-bottom: 0.85rem;
  font-size: 0.83rem;
  color: var(--accent);
  line-height: 1.65;
  text-align: right;
}
.discount-box strong { font-size: 0.95rem; }
.result-btn {
  display: block; width: 100%;
  padding: 0.65rem; border-radius: 8px;
  font-size: 0.86rem; font-weight: 600;
  font-family: 'Heebo', sans-serif;
  border: none; cursor: pointer;
  margin-bottom: 0.45rem;
  text-align: center; text-decoration: none;
}
.btn-gold  { background: var(--accent); color: #111; }
.btn-whatsapp { background: #25D366; color: #fff; }
.btn-dark  { background: #1a1a1a; border: 1px solid #2a2a2a; color: var(--muted); }

/* Loading / error */
#loading, #error-msg {
  padding: 3rem 1rem;
  text-align: center;
  color: var(--muted);
}
</style>
</head>
<body>

<header class="site-header">
  <a class="site-title" href="https://amitphotos.com">Amit Photos</a>
  <span style="color:var(--muted);font-size:0.8rem">🌍 מאיפה הצילום?</span>
</header>

<div class="game-card" id="game-card">
  <div id="loading">טוען תמונות...</div>
  <div id="error-msg" style="display:none">לא הצלחנו לטעון תמונות. <a href="https://amitphotos.com" style="color:var(--accent)">חזרה לגלריה</a></div>

  <!-- Game screen -->
  <div id="game-screen" style="display:none">
    <div class="progress-bar">
      <span class="score-badge" id="score-badge">✓ 0 נכון</span>
      <div class="progress-track"><div class="progress-fill" id="progress-fill" style="width:0%"></div></div>
      <span class="progress-text" id="progress-text">1 / 5</span>
    </div>

    <div class="photo-area">
      <img id="question-img" src="" alt="">
      <div class="photo-overlay"></div>
      <div class="question-label">🌍 מאיפה הצילום?</div>
    </div>

    <div class="photo-desc" id="photo-desc"></div>

    <div class="answers" id="answers"></div>
    <div class="feedback" id="feedback"></div>
    <button class="next-btn" id="next-btn">הבא ←</button>
  </div>

  <!-- Result screen -->
  <div class="result-screen" id="result-screen">
    <div class="win-photo">
      <img id="result-img" src="" alt="">
      <div class="win-badge" id="result-badge">🌍 סיבוב הושלם!</div>
      <div class="win-photo-overlay"></div>
    </div>
    <div class="win-content">
      <div class="win-trophy" id="result-trophy">🏆</div>
      <div class="win-title" id="result-title">כל הכבוד!</div>
      <div class="win-sub" id="result-sub">ענית נכון על 5 מתוך 5</div>
      <div class="win-score" id="win-score"></div>
      <div class="discount-box" id="discount-box" style="display:none">
        🎁 זכית ב<strong>הנחה 20%</strong> על כל תמונה בגלריה<br>
        <small style="color:#a88040">בתוקף 24 שעות מרגע הניצחון</small>
      </div>
      <a class="result-btn btn-gold" id="btn-gallery" href="https://amitphotos.com/?discount=quiz">🛍️ לגלריה עם ההנחה</a>
      <a class="result-btn btn-whatsapp" id="btn-share" href="#" target="_blank">💬 שתף בוואטסאפ</a>
      <button class="result-btn btn-dark" id="btn-retry">🔄 סיבוב חדש</button>
    </div>
  </div>
</div>

<script>
const SITE_URL   = 'https://amitphotos.com';
const QUIZ_URL   = `${SITE_URL}/quiz/`;
const QUESTIONS  = 5;
const WIN_SCORE  = 3;

const COUNTRIES = [
  "🇮🇱 ישראל", "🇮🇹 איטליה", "🇬🇧 אנגליה", "🇬🇷 יוון",
  "🇦🇹 אוסטריה", "🇳🇱 הולנד", "🇩🇪 גרמניה", "🇹🇿 טנזניה",
  "🇲🇪 מונטנגרו", "🇸🇰 סלובקיה", "🇺🇸 ארה״ב", "🇪🇸 ספרד",
  "🇨🇿 צ׳כיה", "🇦🇪 אמירויות"
];

// Map category name → country label (must match COUNTRIES list)
const CATEGORY_COUNTRY = {
  "ישראל": "🇮🇱 ישראל",
  "איטליה": "🇮🇹 איטליה",
  "אנגליה": "🇬🇧 אנגליה",
  "יוון": "🇬🇷 יוון",
  "וינה": "🇦🇹 אוסטריה",
  "הולנד": "🇳🇱 הולנד",
  "גרמניה": "🇩🇪 גרמניה",
  "טנזניה": "🇹🇿 טנזניה",
  "מונטנגרו": "🇲🇪 מונטנגרו",
  "סלובקיה": "🇸🇰 סלובקיה",
  "סן דיאגו": "🇺🇸 ארה״ב",
  "ספרד ואנדורה": "🇪🇸 ספרד",
  "צכיה": "🇨🇿 צ׳כיה",
  "אבו דאבי": "🇦🇪 אמירויות",
};

let questions    = [];  // [{photo, correctCountry, options:[...4 countries]}]
let current      = 0;
let score        = 0;
let results      = [];  // true/false per question
let answered     = false;

function shuffle(arr) {
  const a = [...arr];
  for (let i = a.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [a[i], a[j]] = [a[j], a[i]];
  }
  return a;
}

function buildOptions(correct) {
  const others = shuffle(COUNTRIES.filter(c => c !== correct)).slice(0, 3);
  return shuffle([correct, ...others]);
}

async function loadQuestions() {
  try {
    const resp = await fetch('/api/photos');
    if (!resp.ok) throw new Error('API error');
    const photos = await resp.json();
    const eligible = photos.filter(p => p.quiz_eligible);
    if (eligible.length < QUESTIONS) {
      document.getElementById('error-msg').textContent =
        `לא מספיק תמונות לקוויז (${eligible.length} מתוך ${QUESTIONS} נדרשות).`;
      document.getElementById('error-msg').style.display = '';
      document.getElementById('loading').style.display = 'none';
      return false;
    }
    const selected = shuffle(eligible).slice(0, QUESTIONS);
    questions = selected.map(photo => {
      const correctCountry = CATEGORY_COUNTRY[photo.category] || photo.category;
      return { photo, correctCountry, options: buildOptions(correctCountry) };
    });
    return true;
  } catch(e) {
    document.getElementById('error-msg').style.display = '';
    document.getElementById('loading').style.display = 'none';
    return false;
  }
}

function renderQuestion() {
  const q = questions[current];
  answered = false;

  // Progress
  document.getElementById('progress-text').textContent = `${current + 1} / ${QUESTIONS}`;
  document.getElementById('progress-fill').style.width = `${(current / QUESTIONS) * 100}%`;
  document.getElementById('score-badge').textContent = `✓ ${score} נכון`;

  // Photo
  const img = document.getElementById('question-img');
  img.src = q.photo.thumbnail || q.photo.url;
  img.alt = '';

  // Hide desc + feedback + next btn
  document.getElementById('photo-desc').style.display = 'none';
  const fb = document.getElementById('feedback');
  fb.className = 'feedback';
  fb.style.display = 'none';
  fb.textContent = '';
  document.getElementById('next-btn').style.display = 'none';

  // Render answer buttons
  const answersEl = document.getElementById('answers');
  answersEl.innerHTML = '';
  q.options.forEach(country => {
    const btn = document.createElement('button');
    btn.className = 'answer-btn';
    btn.textContent = country;
    btn.onclick = () => handleAnswer(country, btn);
    answersEl.appendChild(btn);
  });
}

function handleAnswer(chosen, clickedBtn) {
  if (answered) return;
  answered = true;
  const q = questions[current];
  const isCorrect = chosen === q.correctCountry;

  if (isCorrect) {
    score++;
    results.push(true);
    clickedBtn.classList.add('correct');
    document.getElementById('feedback').textContent = '✅ נכון! ניחשת יפה';
    document.getElementById('feedback').className = 'feedback correct';
  } else {
    results.push(false);
    clickedBtn.classList.add('wrong');
    // highlight correct
    document.querySelectorAll('.answer-btn').forEach(b => {
      if (b.textContent === q.correctCountry) b.classList.add('correct');
    });
    document.getElementById('feedback').textContent = `❌ טעות — התשובה הנכונה: ${q.correctCountry}`;
    document.getElementById('feedback').className = 'feedback wrong';
  }

  // Disable all buttons
  document.querySelectorAll('.answer-btn').forEach(b => b.disabled = true);

  // Show description
  if (q.photo.quiz_description) {
    const descEl = document.getElementById('photo-desc');
    // First sentence is bolded (up to first em-dash or comma or end)
    const desc = q.photo.quiz_description;
    const dashIdx = desc.indexOf(' — ');
    if (dashIdx > -1) {
      descEl.innerHTML = `<strong>${desc.slice(0, dashIdx)}</strong>${desc.slice(dashIdx)}`;
    } else {
      descEl.textContent = desc;
    }
    descEl.style.display = '';
  }

  document.getElementById('next-btn').style.display = '';
  document.getElementById('score-badge').textContent = `✓ ${score} נכון`;
}

function nextQuestion() {
  current++;
  if (current >= QUESTIONS) {
    showResult();
  } else {
    renderQuestion();
  }
}

function showResult() {
  document.getElementById('game-screen').style.display = 'none';
  const resultScreen = document.getElementById('result-screen');
  resultScreen.style.display = '';

  const won = score >= WIN_SCORE;

  // Last question's photo as result image
  const lastPhoto = questions[QUESTIONS - 1].photo;
  document.getElementById('result-img').src = lastPhoto.thumbnail || lastPhoto.url;

  document.getElementById('result-trophy').textContent = won ? '🏆' : '📸';
  document.getElementById('result-title').textContent  = won ? 'כל הכבוד!' : 'כמעט!';
  document.getElementById('result-sub').textContent    = `ענית נכון על ${score} מתוך ${QUESTIONS} שאלות`;

  // Score dots
  const dotContainer = document.getElementById('win-score');
  dotContainer.innerHTML = '';
  results.forEach(r => {
    const d = document.createElement('div');
    d.className = `win-dot ${r ? 'ok' : 'bad'}`;
    d.textContent = r ? '✓' : '✗';
    dotContainer.appendChild(d);
  });

  if (won) {
    document.getElementById('discount-box').style.display = '';
    document.getElementById('btn-gallery').style.display = '';
    const waText = encodeURIComponent(`ניצחתי במשחק "מאיפה הצילום?" של עמית! קיבלתי 20% הנחה 🎁 — ${QUIZ_URL}`);
    document.getElementById('btn-share').href = `https://wa.me/?text=${waText}`;
  } else {
    document.getElementById('discount-box').style.display = 'none';
    document.getElementById('btn-gallery').style.display = 'none';
    document.getElementById('result-badge').textContent = '🌍 סיבוב הושלם';
  }

  document.getElementById('btn-retry').onclick = startGame;
}

async function startGame() {
  // Reset state
  current = 0; score = 0; results = []; answered = false;
  document.getElementById('result-screen').style.display = 'none';
  document.getElementById('loading').style.display = '';
  document.getElementById('error-msg').style.display = 'none';
  document.getElementById('game-screen').style.display = 'none';

  const ok = await loadQuestions();
  if (!ok) return;

  document.getElementById('loading').style.display = 'none';
  document.getElementById('game-screen').style.display = '';
  renderQuestion();
}

document.getElementById('next-btn').addEventListener('click', nextQuestion);

startGame();
</script>
</body>
</html>
```

- [ ] **Step 2: Test locally**

```bash
python -m http.server 8000
# Open http://localhost:8000/quiz/
```

Expected: quiz loads with photos (if any are marked quiz_eligible), or shows "not enough photos" message.

To test with mock data, temporarily patch one photo via admin toggle first (Task 4), then reload.

- [ ] **Step 3: Commit**

```bash
git add quiz/index.html
git commit -m "feat: add quiz game page quiz/index.html"
```

---

## Task 6: src/scan_quiz_photos.py — Claude Vision scanner

**Files:**
- Create: `src/scan_quiz_photos.py`

One-time script that fetches all photos from quiz-eligible categories, sends each to Claude Vision, and PATCHes `quiz_eligible` + `quiz_description` back to the API.

- [ ] **Step 1: Create src/scan_quiz_photos.py**

```python
#!/usr/bin/env python3
"""
Scan Quiz Photos — one-time script
Fetches photos from eligible categories, uses Claude Vision to identify
recognizable landmarks, sets quiz_eligible + quiz_description.
"""

import os
import sys
import base64
import requests
import anthropic
import json

SITE_URL = "https://amitphotos.com"
ADMIN_TOKEN = os.environ.get("ADMIN_TOKEN", "")
ANTHROPIC_API_KEY = os.environ.get("ANTHROPIC_API_KEY", "").strip()

ELIGIBLE_CATEGORIES = {
    "ישראל", "איטליה", "אנגליה", "יוון", "וינה", "הולנד", "גרמניה",
    "טנזניה", "מונטנגרו", "סלובקיה", "סן דיאגו", "ספרד ואנדורה", "צכיה", "אבו דאבי",
}

PROMPT = """Does this photo show a recognizable landmark, building, or location that people would clearly identify?
Examples of YES: Eiffel Tower, Colosseum, Big Ben, Western Wall, Santorini buildings, Florence Cathedral.
Examples of NO: flowers, nature without landmarks, abstract, portrait of people, generic street with no recognizable feature.

If YES, return only valid JSON:
{"eligible": true, "description": "Name — brief factual description, City, Country (in Hebrew)"}
Example: {"eligible": true, "description": "הקולוסיאום — אמפיתיאטרון רומי מהמאה הראשונה לספירה. רומא, איטליה."}

If NO, return only:
{"eligible": false}

Return ONLY the JSON, nothing else."""


def load_photos():
    resp = requests.get(f"{SITE_URL}/api/photos", timeout=15)
    resp.raise_for_status()
    photos = resp.json()
    eligible = [p for p in photos if p.get("category") in ELIGIBLE_CATEGORIES]
    print(f"📸 {len(eligible)} תמונות בקטגוריות כשירות")
    return eligible


def fetch_image_b64(url, max_bytes=3_750_000):
    if url.startswith("/"):
        url = f"{SITE_URL}{url}"
    resp = requests.get(url, timeout=30)
    resp.raise_for_status()
    img_bytes = resp.content
    try:
        from PIL import Image
        import io
        img = Image.open(io.BytesIO(img_bytes)).convert("RGB")
        if max(img.size) > 1200:
            img.thumbnail((1200, 1200), Image.LANCZOS)
        quality = 80
        while quality >= 40:
            buf = io.BytesIO()
            img.save(buf, format="JPEG", quality=quality)
            img_bytes = buf.getvalue()
            if len(img_bytes) <= max_bytes:
                break
            quality -= 15
    except ImportError:
        pass
    return base64.standard_b64encode(img_bytes).decode("utf-8")


def analyze_photo(client, photo):
    url = photo.get("thumbnail") or photo.get("url")
    try:
        b64 = fetch_image_b64(url)
    except Exception as e:
        print(f"  ⚠️  הורדה נכשלה ({e})")
        return None

    try:
        msg = client.messages.create(
            model="claude-haiku-4-5-20251001",
            max_tokens=200,
            messages=[{
                "role": "user",
                "content": [
                    {"type": "image", "source": {"type": "base64", "media_type": "image/jpeg", "data": b64}},
                    {"type": "text", "text": PROMPT},
                ]
            }]
        )
        text = msg.content[0].text.strip()
        # Extract JSON even if wrapped in markdown
        start = text.find('{')
        end   = text.rfind('}') + 1
        result = json.loads(text[start:end])
        return result
    except Exception as e:
        print(f"  ⚠️  Claude נכשל ({e})")
        return None


def patch_photo(photo_id, quiz_eligible, quiz_description=""):
    if not ADMIN_TOKEN:
        print("  ⚠️  ADMIN_TOKEN חסר — מדלג על PATCH")
        return
    resp = requests.patch(
        f"{SITE_URL}/api/photos",
        json={"id": photo_id, "quiz_eligible": quiz_eligible, "quiz_description": quiz_description},
        headers={"X-Admin-Password": ADMIN_TOKEN},
        timeout=15,
    )
    if not resp.ok:
        print(f"  ❌ PATCH נכשל: {resp.status_code} {resp.text}")
    else:
        print(f"  ✅ עודכן: eligible={quiz_eligible}")


def main():
    if not ANTHROPIC_API_KEY:
        print("❌ חסר ANTHROPIC_API_KEY")
        sys.exit(1)

    photos = load_photos()
    client = anthropic.Anthropic(api_key=ANTHROPIC_API_KEY)

    marked = 0
    for i, photo in enumerate(photos, 1):
        title = photo.get("title", "ללא כותרת")
        print(f"\n[{i}/{len(photos)}] {title}")

        result = analyze_photo(client, photo)
        if result is None:
            continue

        if result.get("eligible"):
            desc = result.get("description", "")
            print(f"  🌍 מזוהה: {desc}")
            patch_photo(photo["id"], True, desc)
            marked += 1
        else:
            print("  — לא מזוהה")
            # Optionally clear if previously marked (skip by default)

    print(f"\n✅ סריקה הסתיימה — {marked} תמונות סומנו כמתאימות לקוויז")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Test dry-run (no patching)**

```bash
# Comment out the patch_photo() call in main() to test without writing
ANTHROPIC_API_KEY=... ADMIN_TOKEN=... python src/scan_quiz_photos.py
```

Expected output:
```
📸 42 תמונות בקטגוריות כשירות
[1/42] הקולוסיאום
  🌍 מזוהה: הקולוסיאום — אמפיתיאטרון רומי. רומא, איטליה.
[2/42] פרחים בשדה
  — לא מזוהה
...
✅ סריקה הסתיימה — 18 תמונות סומנו כמתאימות לקוויז
```

- [ ] **Step 3: Run for real**

```bash
ANTHROPIC_API_KEY=... ADMIN_TOKEN=... python src/scan_quiz_photos.py
```

- [ ] **Step 4: Commit**

```bash
git add src/scan_quiz_photos.py
git commit -m "feat: add scan_quiz_photos.py — Claude Vision landmark detector"
```

---

## Task 7: Weekly quiz promo — social post + workflow

**Files:**
- Create: `src/quiz_social_post.py`
- Create: `.github/workflows/quiz-social-post.yml`

- [ ] **Step 1: Create src/quiz_social_post.py**

This is identical in structure to `src/puzzle_social_post.py` — copy and adjust for quiz:

```python
#!/usr/bin/env python3
"""
Quiz Promo Post
מפרסם פוסט שבועי לאינסטגרם ופייסבוק עם הזמנה לשחק בקוויז ולקבל 20% הנחה.
"""

import os
import sys
import random
import base64
import requests
import anthropic

SITE_URL  = "https://amitphotos.com"
GRAPH_API = "https://graph.facebook.com/v21.0"
QUIZ_URL  = "https://amitphotos.com/quiz/"

IG_USER_ID        = os.environ.get("INSTAGRAM_USER_ID", "")
IG_TOKEN          = os.environ.get("INSTAGRAM_PAGE_TOKEN", "")
FB_PAGE_ID        = os.environ.get("FACEBOOK_PAGE_ID", "")
FB_TOKEN          = os.environ.get("FACEBOOK_PAGE_TOKEN", "")
ANTHROPIC_API_KEY = os.environ.get("ANTHROPIC_API_KEY", "").strip()
ADMIN_TOKEN       = os.environ.get("ADMIN_TOKEN", "")


def load_quiz_photos():
    try:
        resp = requests.get(f"{SITE_URL}/api/photos", timeout=15)
        resp.raise_for_status()
        photos = resp.json()
        valid = [p for p in photos if p.get("quiz_eligible") and p.get("title")]
        if valid:
            return valid
    except Exception as e:
        print(f"⚠️  API נכשל ({e})")
    sys.exit(1)


def fetch_image_as_base64(url, max_bytes=3_750_000):
    if url.startswith("/"):
        url = f"{SITE_URL}{url}"
    resp = requests.get(url, timeout=30)
    resp.raise_for_status()
    img_bytes = resp.content
    try:
        from PIL import Image
        import io
        img = Image.open(io.BytesIO(img_bytes)).convert("RGB")
        if max(img.size) > 2000:
            img.thumbnail((2000, 2000), Image.LANCZOS)
        quality = 85
        while quality >= 40:
            buf = io.BytesIO()
            img.save(buf, format="JPEG", quality=quality)
            img_bytes = buf.getvalue()
            if len(img_bytes) <= max_bytes:
                break
            quality -= 15
    except ImportError:
        pass
    return base64.standard_b64encode(img_bytes).decode("utf-8"), "image/jpeg"


def generate_caption(photo):
    client = anthropic.Anthropic(api_key=ANTHROPIC_API_KEY)
    title    = photo.get("title", "")
    desc     = photo.get("quiz_description", "")
    category = photo.get("category", "")

    thumbnail_url = photo.get("thumbnail") or photo.get("url")
    if thumbnail_url and thumbnail_url.startswith("/"):
        thumbnail_url = f"{SITE_URL}{thumbnail_url}"

    image_content = []
    try:
        b64, mime = fetch_image_as_base64(thumbnail_url)
        image_content = [{"type": "image", "source": {"type": "base64", "media_type": mime, "data": b64}}]
    except Exception as e:
        print(f"⚠️  Vision נכשל ({e})")

    place_hint = f'"{desc}"' if desc else f'"{title}" (קטגוריה: {category})'

    prompt = f"""Write a social media post for Israeli photographer Amit.
The photo shows: {place_hint}
Quiz URL: {QUIZ_URL}

Post structure:
1. One intriguing question: "מאיפה הצילום הזה?" or a variation — don't reveal the answer.
2. Invite to play the quiz: "נחשו נכון 3 מתוך 5 ותקבלו 20% הנחה על כל תמונה בגלריה"
3. "הלינק בביו / {QUIZ_URL}"

Rules:
- Hebrew only
- Friendly, playful tone
- No hashtags (added separately)
- Output only the post text"""

    msg = client.messages.create(
        model="claude-sonnet-4-6",
        max_tokens=300,
        messages=[{"role": "user", "content": image_content + [{"type": "text", "text": prompt}]}],
    )
    caption = msg.content[0].text.strip()
    hashtags = "#quiz #קוויז #amitphotos #photography #צילום #ישראל #discount #travel"
    return f"{caption}\n\n{hashtags}"


def get_image_url_for_post(photo):
    source_url = photo.get("url") or photo.get("thumbnail")
    if source_url and source_url.startswith("/"):
        source_url = f"{SITE_URL}{source_url}"
    if source_url and source_url.startswith(f"{SITE_URL}/"):
        return source_url

    resp = requests.get(source_url, timeout=30)
    resp.raise_for_status()
    img_bytes = resp.content

    if ADMIN_TOKEN:
        try:
            r = requests.post(
                f"{SITE_URL}/api/admin/upload-story",
                data=img_bytes,
                headers={"Authorization": f"Bearer {ADMIN_TOKEN}", "Content-Type": "image/jpeg"},
                timeout=60,
            )
            if r.ok:
                url = r.json().get("url", "")
                if url:
                    return url
        except Exception as e:
            print(f"⚠️  R2 upload נכשל ({e})")

    r = requests.post("https://0x0.st", files={"file": ("photo.jpg", img_bytes, "image/jpeg")}, timeout=60)
    r.raise_for_status()
    return r.text.strip()


def post_to_instagram(image_url, caption):
    if not IG_USER_ID or not IG_TOKEN:
        print("⚠️  חסרים פרטי אינסטגרם — מדלג")
        return
    import time
    container = requests.post(f"{GRAPH_API}/{IG_USER_ID}/media", data={
        "image_url": image_url, "caption": caption, "access_token": IG_TOKEN,
    }, timeout=30)
    if not container.ok:
        print(f"❌ IG container נכשל: {container.text}")
        return
    cid = container.json().get("id")
    for _ in range(10):
        time.sleep(5)
        status = requests.get(f"{GRAPH_API}/{cid}", params={"fields": "status_code", "access_token": IG_TOKEN}, timeout=30).json().get("status_code", "")
        if status == "FINISHED":
            break
        if status == "ERROR":
            print("❌ Container שגיאה")
            return
    publish = requests.post(f"{GRAPH_API}/{IG_USER_ID}/media_publish", data={"creation_id": cid, "access_token": IG_TOKEN}, timeout=30)
    if publish.ok:
        print(f"✅ פורסם לאינסטגרם! ID: {publish.json().get('id')}")
    else:
        print(f"❌ IG publish נכשל: {publish.text}")


def post_to_facebook(image_url, caption):
    if not FB_PAGE_ID or not FB_TOKEN:
        print("⚠️  חסרים פרטי פייסבוק — מדלג")
        return
    resp = requests.post(f"{GRAPH_API}/{FB_PAGE_ID}/photos", data={
        "url": image_url, "message": caption, "access_token": FB_TOKEN,
    }, timeout=30)
    if resp.ok:
        print(f"✅ פורסם לפייסבוק! ID: {resp.json().get('id')}")
    else:
        print(f"❌ FB post נכשל: {resp.text}")


def main():
    if not ANTHROPIC_API_KEY:
        print("❌ חסר ANTHROPIC_API_KEY")
        sys.exit(1)

    photos = load_quiz_photos()
    photo  = random.choice(photos)
    print(f"📸 תמונה נבחרה: {photo['title']}")

    print("✍️  מייצר כיתוב...")
    caption = generate_caption(photo)
    print(f"\n--- פוסט ---\n{caption}\n-----------\n")

    image_url = get_image_url_for_post(photo)

    print("📤 מפרסם לסושיאל...")
    post_to_instagram(image_url, caption)
    post_to_facebook(image_url, caption)


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Create .github/workflows/quiz-social-post.yml**

```yaml
name: פרסום פרומו קוויז שבועי

on:
  # כל רביעי ב-15:00 UTC = 18:00 ישראל
  schedule:
    - cron: '0 15 * * 3'
  workflow_dispatch:

jobs:
  quiz-promo:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Python setup
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: התקנת חבילות
        run: pip install requests anthropic pillow

      - name: פרסום פרומו קוויז
        env:
          INSTAGRAM_USER_ID: ${{ secrets.INSTAGRAM_USER_ID }}
          INSTAGRAM_PAGE_TOKEN: ${{ secrets.INSTAGRAM_PAGE_TOKEN }}
          FACEBOOK_PAGE_ID: ${{ secrets.FACEBOOK_PAGE_ID }}
          FACEBOOK_PAGE_TOKEN: ${{ secrets.FACEBOOK_PAGE_TOKEN }}
          ANTHROPIC_API_KEY: ${{ secrets.AMIT_PHOTO_AGENT }}
          ADMIN_TOKEN: ${{ secrets.ADMIN_PASSWORD }}
        run: python src/quiz_social_post.py
```

- [ ] **Step 3: Commit**

```bash
git add src/quiz_social_post.py .github/workflows/quiz-social-post.yml
git commit -m "feat: add weekly quiz promo — social post + workflow"
```

---

## Task 8: Push and verify

- [ ] **Step 1: Push all commits**

```bash
git push origin main
```

- [ ] **Step 2: Run scan_quiz_photos manually (one-time)**

In GitHub Actions → Run workflow for any existing workflow, OR locally:

```bash
ANTHROPIC_API_KEY=<key> ADMIN_TOKEN=<password> python src/scan_quiz_photos.py
```

Verify at least 5 photos are marked `quiz_eligible` in the DB before the quiz can run.

- [ ] **Step 3: Open quiz in browser**

Open `https://amitphotos.com/quiz/`

Expected: quiz loads, shows 5 questions, feedback works, win screen shows discount link.

- [ ] **Step 4: Test quiz discount in gallery**

Open `https://amitphotos.com/?discount=quiz`

Expected: gallery banner shows quiz discount text, clicking any photo → buy modal shows 20% off + `🌍 הנחת קוויז` badge on all photos.

- [ ] **Step 5: Trigger social post manually (optional)**

GitHub → Actions → "פרסום פרומו קוויז שבועי" → Run workflow

Expected: post published to Instagram + Facebook with quiz photo + invitation caption.

---

## Self-Review Notes

**Spec coverage:**
- ✅ `quiz/index.html` standalone page — Task 5
- ✅ `src/scan_quiz_photos.py` — Task 6
- ✅ `quiz_eligible` + `quiz_description` DB columns + API — Tasks 1 + 2
- ✅ `gallery.js` `?discount=quiz` → all photos 20% off — Task 3
- ✅ Admin 🌍 badge + copy-link button + double-click toggle — Task 4
- ✅ `.github/workflows/quiz-social-post.yml` + `src/quiz_social_post.py` — Task 7
- ✅ 5 questions, 4 options, immediate feedback + photo description — Task 5
- ✅ 3+/5 → win screen + discount banner — Task 5
- ✅ Lose screen (0-2) → retry without discount — Task 5
- ✅ WhatsApp share button — Task 5

**Type consistency:**
- `quiz_eligible` is always integer (0/1) in DB; `!!p.quiz_eligible` used in JS for boolean coercion ✅
- `CATEGORY_COUNTRY` map keys match the category names in the spec exactly ✅
- `correctCountry` is always a string from `COUNTRIES` array ✅
