# תמונות עם טקסט עברי פגום — נפתר סופית (26.9.2026)

## סיכום מעודכן — סבב שני

בביקורת UX נפרדת (מאוחר יותר באותו יום) עלה שוב ממצא "כותרת פגומה" (`נשר א秃אמריקאי`) שנראה כאילו חזר — התברר שזו לא חזרה, אלא **הסריקה המקורית פספסה סקריפטים שאינם עברית+אנגלית**. הרגקס המקורי בדק רק "עברית צמודה לאנגלית"; תבנית הפגיעה בפועל כללה גם **סינית, קוריאנית, ערבית וקירילית**. סריקה רחבה חדשה (כל תו שאינו עברית/אנגלית/פיסוק נפוץ, בכל שדה) חשפה:

- **10 כותרות נוספות** עם אותה תבנית פגיעה (מגוון סקריפטים)
- **45 שדות description נוספים** עם אותה תבנית פגיעה בדיוק, בכל הרשומות title/category היו תקינים

כל 55 הרשומות הנוספות תוקנו ע"י שחזור טקסט עברי טבעי מתוך `title_en`/`description_en` הנקיים שהיו קיימים, ואומתו מול D1 טרי (עם `?limit=` לעקיפת מטמון). **סה"כ מצטבר: 58 רשומות תוקנו בין שני הסבבים.** סריקה סופית מקיפה על כל 2,311 התמונות המפורסמות (title+category+description, כל הסקריפטים) — **0 שדות חשודים נותרו.**

**שיטת עבודה משופרת:** במקום `curl -d` (שגרם לבאג הקידוד בסבב הראשון), נעשה שימוש בסקריפט Python עם `requests.patch(json=...)` — מטפל ב-UTF-8 נכון אוטומטית, בלי בעיות קידוד.

---

## סיכום מקורי (סבב ראשון)

בזמן ביקורת "שמות תמונות פגומים" נמצאו במקור 3 רשומות עם טקסט עברי פגום, פורסמו (`published=1`) וחיות בפרודקשן. תוך כדי תיקון התגלה **באג קידוד בשיטת השליחה** (לא בשרת) שגרם לניסיון תיקון ראשון להיכשל, ולאחר מכן **סריקה מקיפה חשפה 15 רשומות נוספות** עם אותה תבנית פגיעה — כולן בשדה `description` בלבד. **כל 18 הרשומות תוקנו ואומתו** מול D1 חי (לא מטמון) — 0 שדות חשודים נותרו (בהיקף הרגקס המקורי — ראה עדכון למעלה).

**חשוב:** סיסמת האדמין נמסרה בצ'אט בלבד ולא נשמרה בשום קובץ, commit, או פלט.

---

## באג הקידוד — מצא ותוקן

ניסיון ראשון לתקן תמונה דרך `curl -d '{"...עברית..."}'` (Git Bash ב-Windows) גרם לשרת "לראות" בייטים לא תקינים, לזהות את הכותרת כ"גנרית" (`isGenericTitle`), ולהפעיל יצירה אוטומטית של כותרת חדשה במקום להשתמש בערך שנשלח.

**הפתרון:** כתיבת גוף הבקשה לקובץ UTF-8 מפורש ושליחתו עם `curl --data-binary @file.json` במקום `-d` ישיר. אומת בבייטים (`\xd7\x9e...`), לא בתצוגת טרמינל — הטרמינל של סביבת העבודה הזו לא תמיד מציג עברית נכון גם כשהבייטים בפועל תקינים.

## תגלית נוספת בדרך — מטמון קצה מטעה

אימות ראשוני של אחד התיקונים (Prague) הראה כותרת "פגומה" חדשה (`סילhouette`). בדיקה עם `?limit=` (עוקף את `caches.default` הציבורי ב-`handlePhotos`) הראתה שהערך האמיתי ב-D1 היה נקי לגמרי — התצוגה הפגומה הייתה **תוצאה של מטמון קצה לא מעודכן**, לא נתון אמיתי. **מסקנה תפעולית:** כל אימות עתידי של תוכן ב-`/api/photos` צריך להוסיף `?limit=<מספר גדול>` כדי לעקוף את המטמון הציבורי ולקבל קריאה טרייה מה-D1.

---

## תמונה 1 — גאורגיה (`1Uueu7LeUnN8QsigBP5LtFCHM9Hmxmmfz`) — ✅ תוקן

- **לפני:** `title`: `"מגדלי שומGuards בגאורגיה"`, `title_en` ריק
- **אחרי:** `title`: `"מגדלי שומרים בגאורגיה"` — אומת בבייטים, ללא תווי `�`
- `category`/`description` היו תקינים מלכתחילה, לא שונו
- `title_en` נשאר ריק — יתמלא אוטומטית ע"י ה-workflow הקיים "תרגום כותרות תמונות לאנגלית" (לא הופעל ידנית, כדי לא להריץ פעולת batch רחבה יותר מהנדרש)

## תמונה 2 — בולגריה (`17c9a352-df56-4cea-9a7b-bd326519d043`) — ✅ תוקן

- **לפני:** `title`/`category`/`description` הרוסים לגמרי (תווי `�`)
- **אחרי:** נשלח `title:""` (מפעיל ביודעין את `generateHebrewTitle` — ניתוח Claude Vision על התמונה בפועל) + `category:"בולגריה"` + `description` שנכתב ידנית לפי `title_en`/`description_en` התקינים
- כותרת חדשה שנוצרה אוטומטית: `"אבנים עטופות ירק"` — עברית תקינה לגמרי, אומת בבייטים

## תמונה 3 — פראג (`fa91301c-889f-4ef0-8df4-b6ea85e931e9`) — ✅ תוקן

- **לפני:** `title`/`category`/`description` הרוסים לגמרי
- **אחרי:** אותה שיטה — `title:""` + `category:"פראג"` + `description` ידני
- כותרת חדשה שנוצרה אוטומטית: `"פראג בשעת דמדומים"` — אומת בבייטים אחרי עקיפת מטמון (`?limit=`)

## 15 רשומות נוספות שנמצאו בסריקה מקיפה — ✅ כולן תוקנו

סריקה חוזרת של **כל 1342 התמונות המפורסמות** (לא רק title — גם category ו-description, מול D1 טרי) חשפה תבנית זהה בדיוק ב-`description` בלבד (title/category/title_en/description_en היו **תקינים** בכל 15): מילה עברית שנקטעת באמצע ע"י הזנב האנגלי של אותה מילה (למשל `קactus`←"קקטוס", `דill`←"דיל", `צsiluet`←"צללית"). ב-2 מתוך 15 (אייל על הגבעה) היה כפול — שתי רשומות כמעט-זהות עם אותה פגיעה.

מקור התיקון: `description_en` היה תקין ומלא בכל הרשומות, שימש כבסיס לכתיבת `description` עברי חדש וטבעי (לא תרגום מילולי). `title`/`category`/`title_en` **לא נגעו בהם** — היו תקינים.

רשומות שתוקנו: `58018e1a`, `6c30a3f4`, `9b687de4`, `be33a047`, `2f611edf`, `d0d57a85`, `744fb8cc`, `571450fc`, `6075f013`, `d81ecb43`, `9b073a89`, `17gybzYsCFRqlj4MOtgtwrBdTaMkwg4H8`, `1LCypYbbkyzvaxsSr1UBKXOQf3AsJUjY7`, `f5d12fea`, `d851a737` (מזהי UUID קוצרו לתמצית ראשונה בלבד).

## מקור התקלה — לא באג פעיל

`generateHebrewTitle()` (worker.js) בודקת במפורש שהכותרת שחוזרת מ-Claude מכילה **רק** תווי עברית (`/[^א-תװ-״ ,.\-–—'״׳]/` — דוחה כל תו אחר, כולל אנגלית) — הרצה חיה היום הוכיחה שהבדיקה עובדת נכון (התוצאה הסופית תמיד יצאה נקייה). ה-18 הרשומות המקוריות נוצרו במועד מוקדם יותר, כנראה דרך צינור אחר (Drive-scan/captioning ראשוני) שלא נבדק כאן — אין עדות שהבאג עדיין פעיל היום.

## אימות סופי

```bash
curl "https://amitphotos.com/api/photos?limit=99999"  # עוקף מטמון
```

תוצאה: 1342 תמונות, 0 שדות חשודים (title/category/description) בבדיקת regex + תו replacement.

## אין קשר לתשלומים

כל הבדיקה כללה כתיבות PATCH ל-D1 (18 סה"כ, כולן title/category/description בלבד — אין קשר לתשלומים/הרשמות/סכימה). `PAYMENTS_ENABLED` לא נגע בו איש — נשאר `false`.
