# תמונות עם טקסט עברי פגום — ממצאים והמשך נדרש (26.9.2026)

## סיכום

בזמן ביקורת "שמות תמונות פגומים" (פריט מהרשימה המקורית) נמצאו **3 רשומות שפורסמו (`published=1`) עם טקסט עברי פגום ב-D1** — לא בקובץ הסטטי `data/photos.json` בלבד, אלא חי בפרודקשן, מוצג לגולשים ממש עכשיו (נכון לזמן הבדיקה). זוהה גם ניסיון תיקון אחד שהניב תוצאה לא צפויה עקב באג קידוד (UTF-8) בשיטת השליחה — מתועד למטה כדי לא לחזור עליו.

**חשוב:** אין להשתמש בסיסמת אדמין בשום קובץ, commit, או פלט שנשמר. הסיסמה נמסרה בצ'אט בלבד ולא נשמרה כאן בכוונה.

---

## תמונה 1 — גאורגיה (`1Uueu7LeUnN8QsigBP5LtFCHM9Hmxmmfz`)

**מצב לפני תיקון:**
- `title`: `"מגדלי שומGuards בגאורגיה"` — פגיעה חלקית, ברור שהמילה "שומרים" נדרסה חלקית במילה האנגלית "Guards" (ה-AI כנראה התחיל לתרגם באמצע המילה)
- `title_en`: ריק (`""`)
- `description`: תקין — `"שני מגדלים עתיקים שמורים בעדי יער צפוף בהרי קווקסוס."`
- `description_en`: תקין — `"Two ancient towers, standing sentinel through the dense forest embrace of the Caucasus Mountains."`
- `category`: תקין — `"גאורגיה"`
- `created_at`: 2026-09-25 (תמונה חדשה יחסית, ככל הנראה מ-Drive)

**מה בוצע (26.9.2026):**
נשלח `PATCH /api/photos` עם `X-Admin-Password` תקין ו-body:
```json
{"id":"1Uueu7LeUnN8QsigBP5LtFCHM9Hmxmmfz","title":"מגדלי שומרים בגאורגיה","category":"גאורגיה","description":"שני מגדלים עתיקים שמורים בעדי יער צפוף בהרי קווקסוס."}
```

**תוצאה בפועל:** `{"ok":true,"title":"מגדלים בין עצים"}` — **לא** הכותרת שנשלחה!

**אבחנה:** `handlePhotos` (worker.js, ~שורה 942-951) בודק `isGenericTitle(finalTitle)` (מוגדר ~שורה 1528: `!/[א-ת]/.test(title)` — כותרת נחשבת "גנרית" אם אין בה אף תו עברי). אם הכותרת שנשלחה מגיעה לשרת עם בייטים לא תקינים (סביר: `curl -d '{"...עברית..."}'` דרך Git Bash ב-Windows, שידוע כבעייתי עם UTF-8 בפרמטרים בשורת הפקודה), השרת "רואה" מחרוזת בלי תווים עבריים תקינים → `isGenericTitle` מחזיר `true` → מופעל `generateHebrewTitle()` (קריאת Claude Vision על התמונה בפועל) → נוצרה כותרת חדשה, תקינה אך גנרית, במקום מה שנשלח.

**התוצאה בפועל אינה פגומה** (טקסט עברי תקין ותקף), רק פחות ספציפית ממה שהתכוונתי. `category`/`description` כנראה לא השתנו (לא אומת — קריאת GET לאימות נחסמה על ידי מערכת ההרשאות מיד לאחר הכתיבה, ראה "מה נחסם" למטה).

**לא בוצע עדיין:** אימות של המצב הנוכחי בפועל (title/category/description/title_en) לאחר הכתיבה.

---

## תמונה 2 — בולגריה (`17c9a352-df56-4cea-9a7b-bd326519d043`)

**מצב (טרם טופל כלל):**
- `title`: `"����� ����� �moss ��������"` — **קידוד הרוס לגמרי**, לא רק ערבוב שפות. תווי "�" (U+FFFD) — המידע המקורי אבד, לא ניתן לשחזר מהמחרוזת עצמה.
- `category`: `"�������"` — הרוס גם כן
- `description`: `"����� ������ ������ ���� ���� �� ���� ���� ���"` — הרוס גם כן
- `title_en`: תקין — `"Ancient Moss-Covered Rocks, Bulgaria"`
- `description_en`: `null` (לא קיים בכלל)
- `url`: `/photos/17c9a352-df56-4cea-9a7b-bd326519d043.webp`

**המלצה:** קטגוריה כמעט בוודאות `"בולגריה"` (לפי `title_en` ולפי הדפוס הקיים בדאטהסט — קטגוריות הן שמות מדינות/ערים). לכותרת — עדיף להשתמש במנגנון היצירה האוטומטי הקיים (`generateHebrewTitle`, מריץ Claude Vision על התמונה בפועל) ולא בתרגום-חוזר מ-`title_en`, כדי לקבל כותרת שמתארת את התמונה בפועל ולא תרגום של תרגום. אפשר להשיג את זה ע"י שליחת `title` ריק (`""`) ב-PATCH — `isGenericTitle('')` מחזיר `true` ומפעיל את היצירה האוטומטית ביודעין (בניגוד לתמונה 1, שם זה קרה בטעות).

**`description`** — אין מנגנון אוטומטי מקביל בקוד הזה ל-description; צריך תרגום ידני מ-`description_en` או כתיבה מחדש.

---

## תמונה 3 — פראג (`fa91301c-889f-4ef0-8df4-b6ea85e931e9`)

**מצב (טרם טופל כלל):**
- `title`: `"���houette �� ���� ������"` — הרוס לגמרי (אותה תבנית קידוד שבור)
- `category`: `"����"` — הרוס
- `description`: `"����� �� ����� ���� ������ ����� �� ��� ���"` — הרוס
- `title_en`: תקין — `"Prague Silhouette At Golden Dusk"`
- `description_en`: `null`
- `url`: `/photos/fa91301c-889f-4ef0-8df4-b6ea85e931e9.webp`

**המלצה:** קטגוריה כמעט בוודאות `"פראג"`. אותה גישה כמו תמונה 2 — כותרת דרך `generateHebrewTitle` (title ריק ב-PATCH), description ידני.

---

## מה נחסם

ניסיון `GET /api/photos` לאימות המצב לאחר ה-PATCH הראשון נחסם על ידי מסווג ההרשאות של הסביבה (נראה כממשיך את אותה "פעולה מסומנת" של כתיבה ל-D1 בפרודקשן). לא נעשה ניסיון לעקוף.

## המשך מומלץ (לא בוצע — ממתין להחלטה/סיסמה מחדש)

1. לתקן את בעיית הקידוד לפני כל ניסיון נוסף — קרוב לוודאי צריך לשלוח את גוף הבקשה כ-`--data-binary @file.json` מקובץ שנכתב מראש ב-UTF-8 מפורש, ולא כפרמטר `-d` ישיר בשורת הפקודה (בעיה ידועה עם Git Bash/Windows ותווים לא-ASCII).
2. לאמת מה קרה בפועל לתמונה 1 (title/category/description הנוכחיים).
3. אם תמונה 1 עדיין לא מדויקת מספיק — לתקן שוב (עם הקידוד הנכון הפעם) או להשאיר את "מגדלים בין עצים" (תקינה, רק גנרית).
4. תמונה 2 ותמונה 3 — להריץ PATCH עם קטגוריה נכונה + title ריק (מפעיל יצירה אוטומטית מהתמונה בפועל), ואז להוסיף description בעברית (תרגום/כתיבה ידנית מ-`description_en`).
5. לשקול גם למלא `title_en` לתמונה 1 (כרגע ריק) ו-`description_en` לתמונות 2-3 (כרגע `null`), לעקביות עם שאר הדאטהסט.
6. לבדוק אם קיימות רשומות פגומות נוספות שלא נתפסו ע"י החיפוש (בוצע סריקה לפי תבנית regex של עברית-צמודה-לאנגלית ותווי `�` על כל `data/photos.json` ועל `/api/photos` החי — 3 תוצאות בלבד נמצאו, אך לא בוצעה סריקה שיטתית של כל שדה בכל רשומה).

## אין קשר לתשלומים

כל הבדיקה הזו היא read-only מלבד ה-PATCH האחד שתועד למעלה. `PAYMENTS_ENABLED` לא נגע בו איש — נשאר `false`.
